import React from 'react';

const Homepage = (props) => {
    
    return (
        <div>
            <button>
                Homepage
            </button>
            <button>
                Songs Container
            </button>
        </div>
    );
}

export default Homepage;